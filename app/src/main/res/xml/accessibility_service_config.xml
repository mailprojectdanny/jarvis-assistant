<?xml version="1.0" encoding="utf-8"?>
<!--
  Screen-context understanding is OFF until the user explicitly enables this
  service in Android Settings > Accessibility. It only activates on-demand
  (e.g. "What is this?", "Click the search button") — it does not passively
  log or upload screen content. See ScreenContextReader for the read path.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeViewClicked"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows|flagReportViewIds"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
